// editModal.js

function openEditModal(reservationId, reservationData) {
    // Lógica para abrir el modal de edición con los datos de la cita seleccionada
    console.log(`Modal de edición abierto para la cita con ID: ${reservationId}`);
    console.log('Datos de la cita:', reservationData);
}

// Exporta la función para que esté disponible en otros archivos
export { openEditModal };
